<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Tech-Blog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
  {{!-- Top navbar section --}}
  <nav id="topBar" class="navbar navbar-expand-lg navbar-light bg-dark mb-3">
    <div class="container-fluid">
      <div class="nav-item col-12 col-md-4 display-4">
        <h1 class="navbar-brand">
          <a class="nav-link text-white" href="/">The Tech Blog</a>
        </h1>
      </div>
      <div>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a id="topLinks" class="navbar-brand nav-link text-white" href="/dashboard">Dash</a>
          </li>
          <li class="nav-item">
            {{#if logged_in}}
            <a class="navbar-brand nav-link text-white" id="logout">Logout</a>
            {{else}}
            <a class="navbar-brand nav-link text-white" href="/login">Login</a>
            {{/if}}
          </li>
        </ul>
      </div>
    </div>
  </nav>

  {{!-- Render body layout section --}}
  {{!-- class="container container-fluid mt-5" --}}
  <main>
      {{{ body }}}
  </main>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}

</body>

</html>